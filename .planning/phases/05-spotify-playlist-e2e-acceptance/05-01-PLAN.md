---
phase: 05-spotify-playlist-e2e-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/handler.ts
  - src/youtubeSearcher/index.ts
  - src/youtubeDownloader/index.ts
  - lambda.ts
  - README.md
  - .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md
autonomous: false
requirements:
  - DL-01
requirements_addressed:
  - DL-01
user_setup:
  - "Valid `config/local.json` with Spotify client credentials and `youtubeMp3Downloader` keys"
  - "FFmpeg available per README / config"
  - "Public Spotify playlist URL (tens of tracks); do not commit secrets"
must_haves:
  truths:
    - "Maintainer E2E run is recorded in `05-01-SUMMARY.md` with command, exit code, and output path."
    - "Any code change fixes only DL-01 blockers or doc/CLI mismatches discovered during that run (D-09)."
  artifacts:
    - path: ".planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md"
      provides: "E2E evidence for Phase 5"
      contains: "## E2E run"
  key_links:
    - from: "lambda.ts"
      to: "src/handler.ts"
      via: "handler(argv.url, argv.path, argv.json)"
---

<objective>

Run the **full Spotify playlist CLI pipeline** end-to-end (**DL-01**), record results in **`05-01-SUMMARY.md`**, and apply **minimal** code or README fixes only for issues that **block** DL-01 or contradict documented behavior from Phases **2–4** (per **D-09**).

</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-spotify-playlist-e2e-acceptance/05-CONTEXT.md
@.planning/phases/05-spotify-playlist-e2e-acceptance/05-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@lambda.ts
@src/handler.ts
@src/youtubeSearcher/index.ts
@src/youtubeDownloader/index.ts
@src/sourceScrappers/spotify-scrapper.ts
@README.md
@.cursor/rules/functional-style.mdc
@.cursor/rules/utils-and-modules.mdc
</context>

<tasks>

<task type="manual">
  <name>Task 1: Compile baseline before E2E</name>
  <read_first>
    - package.json
    - .planning/phases/05-spotify-playlist-e2e-acceptance/05-CONTEXT.md
  </read_first>
  <files>N/A (no file edits)</files>
  <action>
    From repository root run **`yarn type-check`**. Do not change source files in this task.
  </action>
  <verify>
    <automated>yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - Command `yarn type-check` exits with code **0**.
  </acceptance_criteria>
  <done>Branch compiles before the E2E run.</done>
</task>

<task type="manual">
  <name>Task 2: Local E2E run and summary artifact</name>
  <read_first>
    - lambda.ts
    - src/handler.ts
    - README.md
    - .planning/phases/05-spotify-playlist-e2e-acceptance/05-CONTEXT.md
  </read_first>
  <files>.planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md</files>
  <action>
    1. Run **`yarn download --url 'https://open.spotify.com/playlist/…'`** from repo root (use a real public playlist URL in place of the ellipsis; **do not** paste secrets into the summary).
    2. Optionally use **`--path ./e2e-output`** (or another writable relative path) to isolate files; if used, mention it under **Command** in the summary.
    3. Create **`.planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md`** containing **exactly** these markdown headings (level 2 and 3 as shown):
       - `## E2E run`
       - `### Command` — one fenced `bash` block or single line with the command used (URL may be redacted as `REDACTED` if needed).
       - `### Prerequisites` — bullet list including Node/yarn, `config/local.json`, FFmpeg.
       - `### Observed` — bullets: track count fetched, YouTube URL count, download summary line or `[execution][finished]` excerpt, list of any errors.
       - `### Exit code` — numeric process exit code.
       - `### Output folder` — absolute or repo-relative path where files appeared (or “none” if failed before download).
       - `### Integration gaps` — bullets: “none” or each gap with **file** + **one-line** fix hypothesis.
  </action>
  <verify>
    <automated>rg "## E2E run" .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md` exists.
    - `rg "## E2E run" .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md` exits **0**.
    - `rg "### Command" .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md` exits **0**.
    - `rg "### Prerequisites" .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md` exits **0**.
    - `rg "### Observed" .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md` exits **0**.
    - `rg "### Exit code" .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md` exits **0**.
    - `rg "### Output folder" .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md` exits **0**.
    - `rg "### Integration gaps" .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md` exits **0**.
  </acceptance_criteria>
  <done>E2E run documented for DL-01 evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Fix blocking integration gaps (if any)</name>
  <read_first>
    - .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md
    - src/handler.ts
    - src/youtubeSearcher/index.ts
    - src/youtubeDownloader/index.ts
    - lambda.ts
    - README.md
  </read_first>
  <files>src/handler.ts, src/youtubeSearcher/index.ts, src/youtubeDownloader/index.ts, lambda.ts, README.md</files>
  <action>
    Read **`### Integration gaps`** in **`05-01-SUMMARY.md`**.
    - If the only bullet is **`none`** (or the section states no code changes needed): **do not** edit `src/` or `lambda.ts`.
    - Otherwise implement the **smallest** change that resolves each **DL-01** blocker listed (silent abort, false empty tracklist, wrong exit handling, CLI flag mismatch vs `lambda.ts`, misleading error with no next step). Do **not** add features outside Phase 5 scope (D-09).
    - **Always** append or replace a **`### Follow-up`** subsection in **`05-01-SUMMARY.md`**: either bullet list of files touched with one line each, or the literal sentence **`No code changes.`** on its own line under that heading.
    - Run **`yarn type-check`** after any edit to `src/` or **`lambda.ts`**; if no such edits, type-check is optional but recommended once before closing the task.
  </action>
  <verify>
    <automated>yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - `yarn type-check` exits **0**.
    - `rg "### Integration gaps" .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md` exits **0**.
    - `rg "### Follow-up" .planning/phases/05-spotify-playlist-e2e-acceptance/05-01-SUMMARY.md` exits **0**.
  </acceptance_criteria>
  <done>Blockers from the E2E run are fixed or explicitly deferred with no src change.</done>
</task>

</tasks>

<verification_criteria>

- `05-01-SUMMARY.md` satisfies all **Task 2** `acceptance_criteria`.
- `yarn type-check` passes after any edits in **Task 3**.

</verification_criteria>
