---
phase: 03-dependency-config-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - package.json
  - yarn.lock
  - README.md
  - config/local.json.example
  - .planning/codebase/STACK.md
autonomous: true
requirements:
  - QUAL-02
requirements_addressed:
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - "Unused direct dependencies (`request`, `request-promise`, `fs` stub, duplicate youtube-mp3-downloader packages) are removed or, if still present transitively only, that intent is documented in `.planning/codebase/STACK.md`."
    - "ESLint-only packages that were incorrectly in `dependencies` are in `devDependencies` when moved (D-07), without breaking `yarn type-check` / `yarn build`."
    - "README contains an authoritative list of every remaining `Config` key with purpose and pipeline stage (D-08); `config/local.json.example` matches `src/types/config.d.ts` with placeholders only (D-09)."
  artifacts:
    - path: "package.json"
      provides: "Trimmed direct deps + eslint packages in devDependencies"
    - path: "README.md"
      provides: "Config keys section referencing example file"
      contains: "Config keys"
    - path: "config/local.json.example"
      provides: "Post–phase-3 template aligned to Config"
    - path: ".planning/codebase/STACK.md"
      provides: "Optional note on transitive legacy deps after trim"
  key_links:
    - from: "README.md Config keys section"
      to: "src/types/config.d.ts"
      via: "Table rows match every interface property path"
    - from: "config/local.json.example"
      to: "README copy/setup instructions"
      via: "Same keys as `Config`"
---

<objective>
Trim **legacy / duplicate direct dependencies** after verification (**D-04**–**D-07**), re-home **ESLint-related** packages to **`devDependencies`** when low-risk, and add the **authoritative config documentation** (**D-08**, **D-09**) in README + example file. **Depends on plan 03-01** so `Config` and README search wording are final before the config key matrix.

Purpose: Finish ROADMAP Phase 3 plan **03-02** and QUAL-02 success criterion (2): contributors know which `config/local.json` keys matter for Spotify, search, download, and paths.

Output: Cleaner `package.json` + `yarn.lock`, updated `README.md`, `config/local.json.example`, optional `STACK.md` note.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dependency-config-cleanup/03-CONTEXT.md
@.planning/codebase/STACK.md
@src/types/config.d.ts
@package.json
@README.md
@config/local.json.example

**Prerequisite:** Execute **03-01** first (`depends_on: ["03-01"]`). **Do not** duplicate 03-01 README work: the **`yt-search`** vs Data API paragraph lives in **03-01**; this plan adds the **config key reference** (**D-08**/**D-09**) only.

<interfaces>
After 03-01, `Config` in `src/types/config.d.ts` contains **`youtubeMp3Downloader`** and **`spotify`** only. The **D-08** table must list **every** nested key path (e.g. `youtubeMp3Downloader.ffmpegPath`, `spotify.clientId`) with one-line purpose and stage label in **`Spotify` | `YouTube search` | `Download` | `Paths / output`** as appropriate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and remove unused direct deps + move ESLint packages (D-04–D-07)</name>
  <read_first>
    - package.json
    - .planning/phases/03-dependency-config-cleanup/03-CONTEXT.md
    - .planning/codebase/STACK.md
    - src/youtubeDownloader/index.ts
  </read_first>
  <files>package.json, yarn.lock, .planning/codebase/STACK.md</files>
  <action>
    **Inventory (before edits)** — run and keep terminal output for your SUMMARY if useful:
    - `rg "from 'request'|from \"request\"|require\\(['\"]request['\"]\\)" src` → must exit **1** before removing deps.
    - `rg "request-promise" src` → must exit **1**.
    - `rg "youtube-mp3-downloader" src` → must exit **1** (stack uses **`ytdl-mp3`** / **`@distube/ytdl-core`** per Phase 2).
    - `rg "from 'fs'|from \"fs\"" src` → expect matches on **Node built-in** `fs` only; the npm package **`fs`** (`0.0.1-security`) is unrelated.

    **D-04:** From `package.json` → `dependencies`, remove **`request`** and **`request-promise`** if the ripgrep checks above show **no** `src/` usage. Run `yarn install`.

    **D-05:** Remove **`youtube-mp3-downloader`** and **`youtube-mp3-downloader-fixed`** from `dependencies` (both package name keys) — neither appears in `src/` and **`ytdl-mp3`** does not list them as dependencies. Run `yarn install`.

    **D-06:** Remove **`fs`** (`^0.0.1-security`) from `dependencies`. Run `yarn install`.

    **D-07:** Move these keys from **`dependencies`** to **`devDependencies`** (merge alphabetically into existing devDeps): **`babel-eslint`**, **`eslint-config-standard`**, **`eslint-plugin-flowtype`**, **`eslint-plugin-import`**. Do **not** upgrade versions. Run `yarn install`.

    **Post-trim verification:** `yarn type-check && yarn build`.

    **Transitive documentation (D-04):** Run `yarn why request` (or `yarn npm why request` per Yarn 4). If any package still pulls `request`, append a short bullet under an existing “Legacy / transitive” style section in **`.planning/codebase/STACK.md`** naming the **direct** package that pulls it (copy a **single line** from `yarn why` output). If `request` is **not** in the tree, add **no** bullet (or one line stating it is absent).
  </action>
  <verify>
    <automated>yarn type-check && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "request-promise|\"request\":" package.json` exits **1** (no direct `request` / `request-promise` keys in `package.json` dependencies **or** devDependencies).
    - `rg "youtube-mp3-downloader" package.json` exits **1**.
    - `rg "\"fs\": \"\\^0\\.0\\.1-security\"" package.json` exits **1**.
    - `rg "babel-eslint" package.json` exits **0** and the match line is under **`devDependencies`**, not **`dependencies`** (use `rg -n "babel-eslint" package.json` and confirm).
    - `yarn build` exits **0**.
  </acceptance_criteria>
  <done>D-04–D-07 applied; lockfile updated; STACK.md updated only when transitive `request` (or similar) remains and needs explanation.</done>
</task>

<task type="auto">
  <name>Task 2: Config keys in README + example template (D-08, D-09)</name>
  <read_first>
    - src/types/config.d.ts
    - README.md
    - config/local.json.example
  </read_first>
  <files>README.md, config/local.json.example</files>
  <action>
    **D-08:** In `README.md`, add a section titled exactly **`## Config keys`**. Directly under the heading, add **one sentence** stating that **no** `config` field is read only for YouTube search after Phase 3 plan **03-01** (search uses **`yt-search`**; point to the existing README subsection from **03-01** by exact heading name). Then add a **markdown table** with columns **`Key`**, **`Purpose (one line)`**, **`Stage`**. Include **one row per leaf field** still present in `src/types/config.d.ts` (e.g. `youtubeMp3Downloader.ffmpegPath` … `spotify.clientSecret`). Map **`Stage`** using **`Spotify`**, **`Download`**, or **`Paths / output`** as appropriate for each key (none map to **`YouTube search`** after **03-01**). End the section with a sentence that the canonical JSON template is **`config/local.json.example`**.

    **D-09:** Ensure **`config/local.json.example`** includes **exactly** the keys required by `Config` (same nesting as `config.d.ts`), placeholder strings only (e.g. keep `<FMPEG_PATH>` style or harmonize with README), **no** `youtubeVideoSearcher` block. If the file already matches after 03-01, add a **single line** at the top of `README` **Configuration Setup** or under **`## Config keys`** pointing to `cp config/local.json.example config/local.json` (do not duplicate long JSON in two places unless README already relies on it — prefer “see example file” for the full blob).

    **Scope boundary:** Do **not** remove or rewrite the **`## YouTube search (QUAL-02)`** subsection added in **03-01**; only add cross-reference from **`## Config keys`** if needed (e.g. “Search uses `yt-search` — see YouTube search section”).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('README.md','utf8');if(!t.includes('## Config keys'))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "^## Config keys" README.md` exits **0**.
    - `node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('config/local.json.example','utf8'));const keys=(o,p='')=>Object.keys(o).flatMap(k=>typeof o[k]==='object'&&o[k]&&!Array.isArray(o[k])?keys(o[k],p+k+'.'):[p+k]);const leaf=keys(j).sort().join(',');const expect=['youtubeMp3Downloader.ffmpegPath','youtubeMp3Downloader.outputPath','youtubeMp3Downloader.progressTimeout','youtubeMp3Downloader.queueParallelism','youtubeMp3Downloader.youtubeVideoQuality','spotify.clientId','spotify.clientSecret'].sort().join(',');if(leaf!==expect)process.exit(1);"` exits **0** (adjust `expect` if `config.d.ts` differs after 03-01 — must match **actual** `Config`).
    - `rg "youtubeVideoSearcher" config/local.json.example` exits **1**.
  </acceptance_criteria>
  <done>D-08 and D-09 satisfied; README and example are aligned with `src/types/config.d.ts`.</done>
</task>

</tasks>

<verification>
`yarn type-check && yarn build`. Optional: `yarn why request` to confirm STACK.md note matches reality.
</verification>

<success_criteria>
Phase 3 **03-02** complete: dependency surface matches verified usage; contributor-facing config map and example file match **`Config`**; QUAL-02 documentation obligations for trim + config clarity met.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dependency-config-cleanup/03-02-SUMMARY.md`
</output>
