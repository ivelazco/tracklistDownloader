---
phase: 03-dependency-config-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/youtubeSearcher/index.ts
  - src/types/config.d.ts
  - package.json
  - yarn.lock
  - README.md
  - config/local.json.example
autonomous: true
requirements:
  - QUAL-02
requirements_addressed:
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - "`src/youtubeSearcher/index.ts` resolves YouTube URLs via `yt-search` only; no `simple-youtube-api` import or `new YouTube(...)` remains."
    - "`Config` in `src/types/config.d.ts` has no `youtubeVideoSearcher` / API key field; tracked `config/local.json.example` matches that shape."
    - "README states explicitly that search uses `yt-search` (not YouTube Data API v3) and no longer requires a Google YouTube API key for search."
  artifacts:
    - path: "src/youtubeSearcher/index.ts"
      provides: "yt-search-only search path + PHASE3 traceability marker"
      contains: "PHASE3-SEARCH"
    - path: "src/types/config.d.ts"
      provides: "Config without misleading YouTube Data API key"
      min_lines: 15
    - path: "package.json"
      provides: "No direct `simple-youtube-api` dependency"
      contains: "dependencies"
  key_links:
    - from: "src/youtubeSearcher/index.ts"
      to: "yt-search npm package"
      via: "yts(track) call"
      pattern: "from 'yt-search'|yts\\("
    - from: "README.md"
      to: "contributor mental model"
      via: "YouTube search explanation + updated Requirements / JSON sample"
      pattern: "yt-search"
---

<objective>
Remove the dead **`simple-youtube-api`** path from **`src/youtubeSearcher/index.ts`**, drop the package from **`package.json`**, and align **`Config`** / tracked example + README so **`youtubeVideoSearcher.apiKey` is not a misleading required key** (**D-01**, **D-02**). Document that URL resolution for search uses **`yt-search`**, not YouTube Data API v3 (**D-03**).

Purpose: Close QUAL-02 for the search dependency surface and satisfy ROADMAP Phase 3 plan **03-01** / success criterion (1): no misleading unused client on the search path.

Output: Updated searcher, `config.d.ts`, `package.json` + lockfile, `README.md`, `config/local.json.example`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dependency-config-cleanup/03-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.cursor/rules/functional-style.mdc
@.cursor/rules/utils-and-modules.mdc
@src/handler.ts
@src/types/config.d.ts
@src/youtubeSearcher/index.ts
@config/local.json.example
@README.md
@package.json

<interfaces>
From `src/youtubeSearcher/index.ts` (pre-change): default export `searchYtVideos(tracklist: string[]): Promise<string[]>`; internal flow `yts(track)` → `findFirstValidVideo` → `prAllYoutubeVideoSearches` → `prAll(responseFormatter)`.

From `src/types/config.d.ts` (pre-change): `youtubeVideoSearcher: { apiKey: string }` must be **removed** per **D-02** once searcher no longer reads it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip simple-youtube-api from youtubeSearcher (D-01)</name>
  <read_first>
    - .planning/phases/03-dependency-config-cleanup/03-CONTEXT.md
    - src/youtubeSearcher/index.ts
    - .cursor/rules/functional-style.mdc
  </read_first>
  <files>src/youtubeSearcher/index.ts</files>
  <action>
    - Delete the line `import YouTube from 'simple-youtube-api';`.
    - Delete `const youtube = new YouTube(config.youtubeVideoSearcher.apiKey);` and any now-unused imports: remove `import configData from '../../config/local.json'`, `import { Config } from '../types/config'`, and `const config = configData as Config` **if** they become unused after removing the API client (search must not read `local.json` for an API key).
    - Keep the **`yt-search`** path unchanged: `import yts from 'yt-search'`, `youtubeVideoSearcher` async function body using `(await yts(track))`, `findFirstValidVideo`, `prAllYoutubeVideoSearches`, `searchYtVideos` export.
    - Add a single-line file comment (immediately after the import block is fine) containing the exact token **`PHASE3-SEARCH`** and the exact substring **`yt-search`** on the same or adjacent line, stating that YouTube URL lookup uses **`yt-search`** only (no YouTube Data API v3 client).
  </action>
  <verify>
    <automated>yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg "simple-youtube-api|new YouTube" src/youtubeSearcher/index.ts` exits **1** (no matches).
    - `rg "PHASE3-SEARCH" src/youtubeSearcher/index.ts` exits **0**.
    - `rg "from 'yt-search'" src/youtubeSearcher/index.ts` exits **0**.
    - `yarn type-check` exits **0**.
  </acceptance_criteria>
  <done>D-01 satisfied in source: dead API client construction removed; traceability marker present.</done>
</task>

<task type="auto">
  <name>Task 2: Config + package + docs for D-02 and D-03</name>
  <read_first>
    - src/types/config.d.ts
    - package.json
    - README.md
    - config/local.json.example
    - src/youtubeSearcher/index.ts
  </read_first>
  <files>src/types/config.d.ts, package.json, yarn.lock, README.md, config/local.json.example</files>
  <action>
    **D-02 — Config and samples**
    - In `src/types/config.d.ts`, remove the entire `youtubeVideoSearcher` property block (lines with `youtubeVideoSearcher`, `apiKey`).
    - In `config/local.json.example`, remove the `youtubeVideoSearcher` JSON object so the file only contains keys that exist on `Config` after cleanup.
    - If your local gitignored `config/local.json` still contains `youtubeVideoSearcher`, delete that property locally so manual runs match the documented shape (do **not** commit secrets).

    **D-01 — package**
    - In `package.json` → `dependencies`, remove the entry `"simple-youtube-api": "^5.2.1"` (exact key `simple-youtube-api`).
    - Run `yarn install` and commit the resulting **`yarn.lock`** change with the same task.

    **D-03 — README (this plan owns the yt-search paragraph; not the full config key matrix — see 03-02 D-08/D-09)**
    - In `README.md` → **Requirements**: remove the bullet that tells users they need a valid **YouTube API Key** / Google Cloud Console key for search (delete the line containing `YouTube API Key` and the link `developers.google.com/youtube/v3` if it is only for search).
    - Add a short subsection (suggested heading: `## YouTube search (QUAL-02)`) that includes **all** of these literal substrings: `yt-search`, `YouTube Data API`, `v3`. Sentence-level meaning must be: track → YouTube URL resolution uses the **`yt-search`** package, **not** YouTube Data API **v3**, so no Google API key is required for that step.
    - Update the **Example configuration** JSON block in `README.md` to **omit** `youtubeVideoSearcher` entirely so it matches `config/local.json.example`.
    - Keep Spotify credential bullets unchanged unless they become redundant.
  </action>
  <verify>
    <automated>yarn type-check && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "youtubeVideoSearcher" src/types/config.d.ts` exits **1**.
    - `rg "simple-youtube-api" package.json` exits **1**.
    - `rg "yt-search" README.md` exits **0**.
    - `rg "YouTube Data API" README.md` exits **0**.
    - `rg "youtubeVideoSearcher" config/local.json.example` exits **1**.
    - `yarn build` exits **0**.
  </acceptance_criteria>
  <done>D-02 and D-03 satisfied; QUAL-02 advanced for search path; lockfile consistent with removed dependency.</done>
</task>

</tasks>

<verification>
`yarn type-check && yarn build`. Optional: `rg "simple-youtube-api" src package.json` returns no matches in `src/` and no direct dep in `package.json`.
</verification>

<success_criteria>
Plan **03-01** complete: no `simple-youtube-api` usage or direct dependency; `Config` and tracked example omit misleading API key; README explains **`yt-search`** vs Data API v3 per **D-03**.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dependency-config-cleanup/03-01-SUMMARY.md`
</output>
