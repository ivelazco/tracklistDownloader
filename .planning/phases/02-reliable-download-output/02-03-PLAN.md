---
phase: 02-reliable-download-output
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/folderManager/index.ts
  - src/handler.ts
  - lambda.ts
  - README.md
autonomous: true
requirements:
  - DL-03
requirements_addressed:
  - DL-03
user_setup: []
must_haves:
  truths:
    - "Playlist folder names use Spotify- and 1001tracklists- prefixes with extended Windows-safe sanitization (D-06)."
    - "`--path` overrides per-run output directory; directory is created recursively when missing (D-05)."
    - "README documents output root, naming, collision reuse, and matches code (D-07)."
  artifacts:
    - path: "src/folderManager/index.ts"
      provides: "sanitizePlaylistFolderName + existing collision reuse"
    - path: "src/handler.ts"
      provides: "--path override flow after createFolder"
    - path: "lambda.ts"
      provides: "yargs --path documented/typed if needed"
    - path: "README.md"
      provides: "## Output layout (DL-03)"
      contains: "DL-03"
  key_links:
    - from: "src/handler.ts"
      to: "src/folderManager/index.ts"
      via: "createFolder then finalPath = path || folderPath"
    - from: "src/folderManager/index.ts"
      to: "config.youtubeMp3Downloader.outputPath"
      via: "path.join outputPath + folderName"
---

<objective>
Harden **folder naming** and **output root** behavior for Spotify and 1001tracklists sources (**DL-03**): sanitize names per **D-06**; ensure **`--path`** is a per-run output directory override with recursive creation per **D-05**; document **collision reuse** per **D-07** and align README folder examples with code (`Spotify-…`, `1001tracklists-…`).

Purpose: Meet ROADMAP Phase 2 success criterion (3) “output path + folder naming documented and predictable.”

Output: Updated `folderManager`, `handler` (and `lambda.ts` if flag wiring gaps exist), README **DL-03** section.

**Execution order:** Assumes plan **02-02** is already merged on the branch (FFmpeg preflight + first-pass README DL-02); this plan extends `handler.ts` and `README.md` after that baseline to avoid merge races.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-reliable-download-output/02-CONTEXT.md
@.planning/phases/02-reliable-download-output/02-RESEARCH.md
@src/folderManager/index.ts
@src/handler.ts
@lambda.ts
@src/youtubeDownloader/index.ts
@.cursor/rules/functional-style.mdc

<interfaces>
From `src/folderManager/index.ts` (pre-phase):
```typescript
async function getFolderName(url: string): Promise<string> {
  if (url.includes('open.spotify.com/playlist')) {
    const playlistName = await getSpotifyPlaylistName(url);
    return `Spotify-${playlistName.replace(/ /g, '-')}`;
  }
  return `1001tracklists-${getRegularFolderName(url).replace(/ /g, '-')}`;
}
```

From `src/handler.ts`:
```typescript
const folderPath = await createFolder(url);
const finalPath = path || folderPath || '';
const result = await ytDownloader(youtubeUrls, finalPath);
```

From `lambda.ts`: yargs argv for `--path` / `--url` (confirm actual flag name is `--path`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sanitizePlaylistFolderName + apply in folderManager</name>
  <read_first>
    - src/folderManager/index.ts
    - .planning/phases/02-reliable-download-output/02-CONTEXT.md
    - .cursor/rules/functional-style.mdc
  </read_first>
  <files>src/folderManager/index.ts</files>
  <behavior>
    - Given playlist name `a/b:c`, sanitized segment must not contain Windows-forbidden characters `< > : " / \\ | ? *`.
    - Given name with leading/trailing spaces or trailing `.`, sanitized output trims unsafe edges per Windows rules.
    - Empty or all-stripped names fall back to a non-empty safe token (e.g. `playlist` or `tracklist`) — choose one constant and document in code comment.
  </behavior>
  <action>
    Introduce a **pure** helper **`sanitizePlaylistFolderName(raw: string): string`** in **`src/folderManager/index.ts`** (or `src/utils` if reused — prefer **local** unless second caller appears).

    Apply it to **both** Spotify playlist titles and 1001 slugs **after** existing space-to-hyphen normalization (order: normalize spaces → sanitize forbidden chars → trim dots/spaces).

    Preserve prefixes exactly: **`Spotify-`** and **`1001tracklists-`** (no spaces around hyphen) per **D-06**.

    Keep **collision behavior**: if folder exists, **reuse** (existing `else` branch logs); do **not** add numeric suffixes (**D-07**).

    Use **`console.log` / `console.error` with `[createFolder]`** namespace for any new messages (GOV-03).
  </action>
  <verify>
    <automated>yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg "sanitizePlaylistFolderName" src/folderManager/index.ts` exits 0.
    - `rg "Spotify-" src/folderManager/index.ts` exits 0 and `rg "1001tracklists-" src/folderManager/index.ts` exits 0.
    - `rg "WINDOWS_FORBIDDEN|forbidden" src/folderManager/index.ts` exits 0 (constant or comment documenting `<>:"/\\|?*`).
    - `yarn type-check` exits 0.
  </acceptance_criteria>
  <done>D-06 satisfied; folder names stable on Windows; prefixes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: handler --path override + recursive mkdir for finalPath</name>
  <read_first>
    - src/handler.ts
    - src/youtubeDownloader/index.ts
    - lambda.ts
  </read_first>
  <files>src/handler.ts, lambda.ts</files>
  <action>
    **`src/handler.ts`:**
    - When CLI **`path`** argument is **truthy**, set **`finalPath`** to **`path.resolve(process.cwd(), path)`** if `path` is relative; if already absolute, use as-is (document choice in one comment line).
    - Before calling **`ytDownloader`**, ensure **`finalPath`** directory exists: **`fs.mkdirSync(finalPath, { recursive: true })`** when override is used (D-05). Avoid double-create errors (mkdirSync recursive is idempotent).
    - When **`path` override is used**, keep calling **`createFolder(url)`** if required for side effects — **do not remove** without replacing equivalent behavior; add **`[handler]`** log: `finalPath` and whether override is active (substring `path override` in message).

    **`lambda.ts`:**
    - Verify yargs option for path uses **`'path'`** as property and **`--path`** in docs string; if missing or typo, fix so `handler(argv.path, ...)` binding is correct. Grep anchor: **`--path`** appears in options definition.

    Import **`fs`** and **`path`** in `handler.ts` only if not present; prefer Node **`path`** module for resolve.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "mkdirSync\\(.*recursive:\\s*true" src/handler.ts` exits 0.
    - `rg "path override|path\\.resolve" src/handler.ts` exits 0.
    - `rg "--path" lambda.ts` exits 0.
    - `yarn build` exits 0.
  </acceptance_criteria>
  <done>D-05 `--path` behavior explicit and creates target directory; lambda flag verified.</done>
</task>

<task type="auto">
  <name>Task 3: README Output layout (DL-03) + collision reuse (D-07)</name>
  <read_first>
    - README.md
    - src/folderManager/index.ts
    - src/handler.ts
  </read_first>
  <files>README.md</files>
  <action>
    Add heading **exactly** `## Output layout (DL-03)`.

    Replace wrong examples: README must **not** claim `Spotify - [` or `1001tracklists - [` with spaces; use **`Spotify-{playlist_name}`** and **`1001tracklists-{slug}`** (literal braces okay as documentation pattern) matching **code**.

    Document:
    - **`outputPath`** from `config/local.json` is the **root** for playlist folders.
    - **`--path`** overrides the download directory for that run (D-05) — mention **`yarn download --path`** example with a sample relative path.
    - **Collision:** if target folder already exists, runs **reuse** it (**D-07**); phrase must include **`reuse`** and **`existing`**.

    If plan 02-02 added a pointer to this section, ensure anchor exists after this edit.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),t=fs.readFileSync('README.md','utf8');['## Output layout (DL-03)','outputPath','--path','reuse','Spotify-','1001tracklists-'].forEach(s=>{if(!t.includes(s))process.exit(1);});if(/Spotify - \\[|1001tracklists - \\[/.test(t))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "## Output layout \\(DL-03\\)" README.md` exits 0.
    - `rg "Spotify - \\[" README.md` exits 1 (old pattern removed).
    - `rg "1001tracklists - \\[" README.md` exits 1 (old pattern removed).
  </acceptance_criteria>
  <done>DL-03 and D-07 documented; README matches folderManager prefixes.</done>
</task>

</tasks>

<verification>
`yarn type-check` && `yarn build`. Manual: run with `--path ./tmp-dl` relative path; folder created and MP3s land there; rerun to confirm reuse messaging still sane.
</verification>

<success_criteria>
ROADMAP Phase 2 DL-03 satisfied; CONTEXT D-05, D-06, D-07 reflected in code + README; depends on 02-01 downloader contract being stable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliable-download-output/02-03-SUMMARY.md`
</output>

## PLANNING COMPLETE
