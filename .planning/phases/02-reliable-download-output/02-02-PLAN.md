---
phase: 02-reliable-download-output
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/utils/ffmpegPreflight.ts
  - src/utils/index.ts
  - src/handler.ts
  - README.md
autonomous: true
requirements:
  - DL-02
requirements_addressed:
  - DL-02
user_setup: []
must_haves:
  truths:
    - "ffmpegPath is validated before downloads start (D-08); missing/invalid binary yields an error naming youtubeMp3Downloader.ffmpegPath."
    - "README documents FFmpeg install, ffmpegPath, outputPath, queueParallelism, and honest ytdl-mp3 vs ffmpegPath behavior (D-09)."
  artifacts:
    - path: "src/utils/ffmpegPreflight.ts"
      provides: "Early FFmpeg executable probe"
    - path: "src/handler.ts"
      provides: "Calls preflight before ytDownloader"
      contains: "assertFfmpegAvailable"
    - path: "README.md"
      provides: "DL-02 config documentation"
      contains: "DL-02"
  key_links:
    - from: "src/handler.ts"
      to: "src/utils/ffmpegPreflight.ts"
      via: "imported assert helper before createFolder/ytDownloader chain"
    - from: "README.md"
      to: "config/local.json keys"
      via: "documented youtubeMp3Downloader fields"
---

<objective>
Validate **`config.youtubeMp3Downloader.ffmpegPath`** early in the download pipeline (**D-08**) with **actionable** errors (key name, expected executable, typical fix). Align **README** with real config keys and document that **`ytdl-mp3` uses bundled `ffmpeg-static`** for transcoding unless the **yt-dlp** strategy from plan 02-01 is active (**D-09**, per 02-RESEARCH.md).

Purpose: Close the DL-02 gap between user expectations and library behavior; support ROADMAP success criterion (1) “documented strategy.”

Output: `src/utils/ffmpegPreflight.ts`, barrel export, `handler` wiring, README **DL-02** section.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-reliable-download-output/02-CONTEXT.md
@.planning/phases/02-reliable-download-output/02-RESEARCH.md
@src/types/config.d.ts
@config/local.json.example
@src/handler.ts
@src/youtubeDownloader/index.ts
@.cursor/rules/utils-and-modules.mdc

<interfaces>
From `src/types/config.d.ts`:
```typescript
youtubeMp3Downloader: {
  ffmpegPath: string;
  outputPath: string;
  youtubeVideoQuality: string;
  queueParallelism: number;
  progressTimeout: number;
};
```

Export from plan 02-01: `PHASE2-DECISION` in `src/youtubeDownloader/index.ts` determines whether README must emphasize **yt-dlp** + `ffmpegPath` vs **ytdl-mp3 + ffmpeg-static**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: assertFfmpegAvailable helper + barrel export</name>
  <read_first>
    - src/types/config.d.ts
    - src/utils/index.ts
    - .cursor/rules/utils-and-modules.mdc
    - .planning/phases/02-reliable-download-output/02-RESEARCH.md
  </read_first>
  <files>src/utils/ffmpegPreflight.ts, src/utils/index.ts</files>
  <action>
    Create **`src/utils/ffmpegPreflight.ts`** exporting a function named exactly **`assertFfmpegAvailable`** with signature `(ffmpegPath: string): void`.

    Implementation requirements:
    - If `!ffmpegPath` or non-string after trim, throw **`Error`** whose **message includes all** substrings: `youtubeMp3Downloader.ffmpegPath`, `config/local.json`, `ffmpeg`.
    - Use **`fs.existsSync`** on the path; if missing, throw **`Error`** whose message includes: `youtubeMp3Downloader.ffmpegPath`, `not found`, and the **resolved or raw path string**.
    - Probe executability with **`child_process.spawnSync(ffmpegPath, ['-version'], { timeout: 8000, encoding: 'utf8', windowsHide: true })`** (adjust only if platform API demands; keep `['-version']`).
    - If `status !== 0` or `error`, throw **`Error`** whose message includes: `ffmpeg`, `-version`, and `youtubeMp3Downloader.ffmpegPath`.

    Re-export **`assertFfmpegAvailable`** from **`src/utils/index.ts`** (named export).

    No `console.log` inside the helper — throw only; caller logs if needed.
  </action>
  <verify>
    <automated>yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export.*assertFfmpegAvailable|assertFfmpegAvailable" src/utils/index.ts src/utils/ffmpegPreflight.ts` exits 0.
    - `rg "spawnSync\\(.*-version" src/utils/ffmpegPreflight.ts` exits 0.
    - `rg "youtubeMp3Downloader\\.ffmpegPath" src/utils/ffmpegPreflight.ts` exits 0.
    - `yarn type-check` exits 0.
  </acceptance_criteria>
  <done>Preflight helper exists, is barrel-exported, and encodes D-08 message requirements.</done>
</task>

<task type="auto">
  <name>Task 2: handler calls assertFfmpegAvailable before downloads</name>
  <read_first>
    - src/handler.ts
    - config/local.json.example
    - src/utils/index.ts
  </read_first>
  <files>src/handler.ts</files>
  <action>
    Import **`assertFfmpegAvailable`** from **`./utils`** (barrel).

    Load **`config.youtubeMp3Downloader.ffmpegPath`** using the same pattern as other modules (**static `config/local.json` import** via existing config access — if handler does not yet import config, import `configData` from `../config/local.json` and cast to `Config` from `src/types/config.d.ts`).

    Call **`assertFfmpegAvailable(config.youtubeMp3Downloader.ffmpegPath)`** **immediately before** the log line `[handler] Starting downloads...` (or before `ytDownloader` is invoked — must run **after** YouTube URL resolution succeeds so failures are not mistaken for search errors).

    Do **not** reorder Phase 2 folder creation semantics beyond what is required for this call; plan 02-03 owns `--path` refinements.
  </action>
  <verify>
    <automated>yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "assertFfmpegAvailable" src/handler.ts` exits 0.
    - `rg "ytDownloader\\(" src/handler.ts` matches a line **after** the `assertFfmpegAvailable` call (visual order in file).
    - `yarn build` exits 0.
  </acceptance_criteria>
  <done>FFmpeg path fails fast with D-08-compliant errors before download phase.</done>
</task>

<task type="auto">
  <name>Task 3: README DL-02 — keys, FFmpeg install, ytdl-mp3 vs ffmpegPath truth</name>
  <read_first>
    - README.md
    - src/types/config.d.ts
    - config/local.json.example
    - src/youtubeDownloader/index.ts
  </read_first>
  <files>README.md</files>
  <action>
    Add a Markdown section heading **exactly** `## Download strategy (DL-02)` (grep anchor).

    Body must include **literal** substrings:
    - `youtubeMp3Downloader`
    - `ffmpegPath`
    - `outputPath`
    - `queueParallelism`
    - `youtubeVideoQuality`
    - `progressTimeout`
    - `ytdl-mp3`
    - `ffmpeg-static`

    Content requirements:
    - Explain that **`queueParallelism` caps concurrent downloads in application code** (not inside `ytdl-mp3`).
    - State clearly: **default `ytdl-mp3` path uses bundled `ffmpeg-static`** for encoding; **`ffmpegPath` in config is still validated** for pipeline contract and for **`yt-dlp`** strategy if `STRATEGY=yt-dlp` appears in `src/youtubeDownloader/index.ts`.
    - Link or bullet **FFmpeg install** guidance (keep brief; may reuse existing README FFmpeg link).
    - Mention **`YTDL_NO_UPDATE`** is set in **`lambda.ts`** before imports (grep-able phrase `lambda.ts`).

    Update **Output Structure** bullets only if they duplicate wrong patterns — **full folder naming correction** is plan **02-03**; here you may add a one-line pointer: “See `## Output layout (DL-03)`” only if 02-03 will add that heading (if not yet present, omit the pointer to avoid broken anchor).

    Fix **example JSON** in README if it contradicts `config/local.json.example` for any **`youtubeMp3Downloader`** key names.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),t=fs.readFileSync('README.md','utf8');['## Download strategy (DL-02)','youtubeMp3Downloader','ffmpegPath','queueParallelism','ytdl-mp3','ffmpeg-static','lambda.ts'].forEach(s=>{if(!t.includes(s))process.exit(1);});"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "## Download strategy \\(DL-02\\)" README.md` exits 0.
    - `rg "progressTimeout" README.md` exits 0.
    - `yarn build` still exits 0 (no TS changes required but sanity).
  </acceptance_criteria>
  <done>README satisfies D-09 and DL-02 traceability anchor.</done>
</task>

</tasks>

<verification>
`yarn type-check` && `yarn build`. Manual: temporarily set `ffmpegPath` to a non-existent path in `config/local.json` and run `yarn download --url ...` — stderr/error must mention `youtubeMp3Downloader.ffmpegPath`.
</verification>

<success_criteria>
D-08 and D-09 satisfied; DL-02 documentation and preflight are grep-verifiable and aligned with 02-RESEARCH honesty table.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliable-download-output/02-02-SUMMARY.md`
</output>

## PLANNING COMPLETE
