---
phase: 02-reliable-download-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/youtubeDownloader/index.ts
autonomous: true
requirements:
  - DL-02
requirements_addressed:
  - DL-02
user_setup: []
must_haves:
  truths:
    - "Executor has audited ytdl-mp3 / @distube/ytdl-core usage vs config keys and recorded it grep-verifiably in-repo."
    - "Parallel downloads are bounded by youtubeMp3Downloader.queueParallelism (D-04) while per-track failures remain visible (D-03)."
    - "Primary fetch path remains ytdl-mp3-centric per D-01 unless a grep-verifiable PHASE2-DECISION documents yt-dlp fallback activation."
  artifacts:
    - path: "src/youtubeDownloader/index.ts"
      provides: "Bounded-concurrency download batch + audit/decision comment block"
      contains: "PHASE2-AUDIT"
  key_links:
    - from: "src/youtubeDownloader/index.ts"
      to: "config/local.json via Config import"
      via: "queueParallelism read for limiter"
    - from: "src/youtubeDownloader/index.ts"
      to: "printResults + groupByStatus"
      via: "combined Promise.allSettled results passed to printResults(folderPath)"
---

<objective>
Audit `youtubeDownloader` and transitive `ytdl-mp3` / `ytdl-core` behavior; keep **ytdl-mp3** as the default implementation path per **D-01**; if the audit proves the stack unmaintainable for v1, activate **D-02** (`yt-dlp` subprocess or hybrid) in the same module and document the final choice. Implement an **application-level concurrency ceiling** aligned with **`youtubeMp3Downloader.queueParallelism`** per **D-04** (library does not honor it — see 02-RESEARCH.md). Preserve **settled aggregation** and per-track failure visibility per **D-03**.

Purpose: Satisfy ROADMAP Phase 2 success criteria (1–2) for DL-02: maintainable strategy and no silent unbounded fan-out.

Output: Hardened `src/youtubeDownloader/index.ts` with grep-verifiable audit/decision markers and bounded parallel downloads.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reliable-download-output/02-CONTEXT.md
@.planning/phases/02-reliable-download-output/02-RESEARCH.md
@.cursor/rules/functional-style.mdc
@.cursor/rules/memory-and-concurrency.mdc
@.cursor/rules/utils-and-modules.mdc
@src/utils/index.ts
@src/utils/printUtils.ts
@src/types/config.d.ts
@lambda.ts

<interfaces>
From `src/utils/index.ts`:
```typescript
const prAll = <T, R>(fnReturn: PromiseSettledHandler<T, R>) => (ps: Promise<T>[]) =>
  Promise.allSettled(ps).then(fnReturn);
export { prAll, printResults, /* ... */ };
```

From `src/utils/printUtils.ts`:
```typescript
const printResults = <T extends { outputFile?: string } | string>(folderPath: string) =>
  compose(printEndOfExecution<T>(folderPath), groupByStatus<T>);
// Expects Array<SettledResult<T>> — same shape as Promise.allSettled output
```

From `src/youtubeDownloader/index.ts` (pre-phase): `new Downloader({ ...config.youtubeMp3Downloader, outputDir: folderPath })`, `transformedVideos.map` → `dl.downloadSong(url)` → `prAll(printResults(folderPath))(promises)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record PHASE2 audit + strategy decision in youtubeDownloader</name>
  <read_first>
    - .planning/phases/02-reliable-download-output/02-RESEARCH.md
    - .planning/phases/02-reliable-download-output/02-CONTEXT.md
    - node_modules/ytdl-mp3/dist/index.d.ts
    - src/youtubeDownloader/index.ts
  </read_first>
  <files>src/youtubeDownloader/index.ts</files>
  <action>
    Add a single block comment at the **top of the file** (after imports is acceptable if TS disallows before imports) titled exactly `PHASE2-AUDIT` containing **all** of these literal substrings for traceability:
    - `ytdl-mp3`
    - `@distube/ytdl-core`
    - `ffmpeg-static`
    - `ffmpegPath`
    - `queueParallelism`
    - `YTDL_NO_UPDATE`
    - `package.json resolutions`

    Include one line stating the **default path is ytdl-mp3** per **D-01**.

    Add a second marker comment block titled exactly `PHASE2-DECISION` containing **exactly one** of:
    - `STRATEGY=ytdl-mp3-only` — if audit concludes the current stack is maintainable for v1 with limiter + pins; **or**
    - `STRATEGY=yt-dlp` — if **D-02** is triggered (document in the same block **one** concrete trigger met, e.g. repeated signature failures or inability to cap concurrency safely).

    If `STRATEGY=yt-dlp`, outline in the block the **entry function name** you will implement in Task 2 (e.g. `downloadViaYtDlp`) and that `ffmpegPath` + `queueParallelism` apply to that path per RESEARCH. If `STRATEGY=ytdl-mp3-only`, state explicitly that **`ytdl-mp3` transcodes via bundled `ffmpeg-static`** and `config.youtubeMp3Downloader.ffmpegPath` is **not** consumed by the library (honesty for D-08/README in plan 02-02).

    Do **not** edit `README.md` in this plan (owned by 02-02/02-03).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='src/youtubeDownloader/index.ts',t=fs.readFileSync(p,'utf8');['PHASE2-AUDIT','ytdl-mp3','ffmpeg-static','ffmpegPath','queueParallelism','PHASE2-DECISION'].forEach(s=>{if(!t.includes(s))process.exit(1);});const m=t.match(/STRATEGY=ytdl-mp3-only|STRATEGY=yt-dlp/g);if(!m||m.length!==1)process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "PHASE2-AUDIT" src/youtubeDownloader/index.ts` exits 0.
    - Automated verify script in Task 1 `<verify>` exits 0 (enforces **exactly one** `STRATEGY=ytdl-mp3-only` or `STRATEGY=yt-dlp` occurrence in the file).
    - `yarn type-check` exits 0.
  </acceptance_criteria>
  <done>Audit and decision are grep-verifiable in `src/youtubeDownloader/index.ts`; D-01 default and D-02 escape hatch are explicit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bounded concurrency + primary download path implementation</name>
  <read_first>
    - src/youtubeDownloader/index.ts
    - src/utils/printUtils.ts
    - src/types/config.d.ts
    - .cursor/rules/memory-and-concurrency.mdc
    - .cursor/rules/functional-style.mdc
  </read_first>
  <files>src/youtubeDownloader/index.ts</files>
  <action>
    **If `PHASE2-DECISION` is `STRATEGY=ytdl-mp3-only` (D-01):**
    - Replace the unbounded `transformedVideos.map(() => dl.downloadSong(...))` fan-out with a **sliding window** or **chunked** execution such that at most **`N` concurrent** `downloadSong` calls run, where `N = Math.max(1, Number(config.youtubeMp3Downloader.queueParallelism) || 1)`.
    - For each finished window, use **`Promise.allSettled`** on that window’s promises only. **Concatenate** all `PromiseSettledResult` objects **in URL order** into one array.
    - Invoke **`printResults(folderPath)(combinedSettledArray)`** and return `Promise.resolve(...)` so callers still get a Promise. Do **not** use raw `Promise.all` over the full playlist. Preserve existing **per-URL** `.then` / `.catch` logging on each `downloadSong` promise **or** equivalent log lines so failures remain visible per track (D-03).
    - Do **not** remove `prAll` import if still used elsewhere; if unused, remove unused import and run type-check.

    **If `PHASE2-DECISION` is `STRATEGY=yt-dlp` (D-02):**
    - Implement subprocess-based downloads using the **`yt-dlp`** executable (document in comment: user must install `yt-dlp` on PATH or configure full path in a **new** optional config key only if you add it to `config.d.ts` + `local.json.example` — prefer `spawn('yt-dlp', ...)` with clear error if missing).
    - Use **`config.youtubeMp3Downloader.ffmpegPath`** as `--ffmpeg-location` (or equivalent flag) where applicable.
    - Apply the **same concurrency ceiling** as above.
    - Aggregate results into `Promise.allSettled`-compatible **manually constructed** fulfilled/rejected results **or** use small adapter objects — final step must call **`printResults(folderPath)`** with a structure **`groupByStatus` accepts** (mirror fulfilled/rejected shape from `Promise.allSettled`).

    Log prefix remains **`[ytDownloader]`** for new messages.
  </action>
  <verify>
    <automated>yarn type-check && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "queueParallelism" src/youtubeDownloader/index.ts` exits 0.
    - `rg "Promise\\.all\\(" src/youtubeDownloader/index.ts` exits 1 **or** if found, only inside chunked windows with size tied to `queueParallelism` (no `Promise.all` over full `transformedVideos.length`); manual review via `rg -n "Promise.all" src/youtubeDownloader/index.ts`.
    - `yarn build` exits 0.
  </acceptance_criteria>
  <done>Downloads respect D-04 cap; D-03 visibility preserved; strategy in code matches `PHASE2-DECISION`.</done>
</task>

</tasks>

<verification>
`yarn type-check` and `yarn build`. Optional manual: run `yarn download` with a 2–3 track list and confirm logs show bounded overlap (not all tracks starting at once) when `queueParallelism` is set to `1` or `2`.
</verification>

<success_criteria>
ROADMAP Phase 2 DL-02 (maintainable acquisition + bounded parallelism) advanced: audit on disk, decision explicit, implementation matches decision; aligns with CONTEXT D-01, D-03, D-04.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliable-download-output/02-01-SUMMARY.md`
</output>

## PLANNING COMPLETE
