---
phase: 04-spotify-api-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/api.d.ts
  - src/sourceScrappers/spotify-scrapper.ts
autonomous: true
requirements:
  - SPOT-01
requirements_addressed:
  - SPOT-01
user_setup: []
must_haves:
  truths:
    - "`getSpotifyPlaylistTracksInternal` requests playlist pages until all tracks are retrieved (no single-page truncation for typical large playlists)."
    - "Removed or null `track` entries from the API are skipped so they do not produce empty strings in the result list."
  artifacts:
    - path: "src/sourceScrappers/spotify-scrapper.ts"
      provides: "Paged `getPlaylistTracks` loop with limit 100"
      contains: "getPlaylistTracks"
    - path: "src/types/api.d.ts"
      provides: "Playlist tracks page metadata optional fields"
      min_lines: 35
  key_links:
    - from: "src/sourceScrappers/spotify-scrapper.ts"
      to: "Spotify Web API playlist tracks"
      via: "getPlaylistTracks(playlistId, { limit, offset })"
      pattern: "limit:\\s*100|offset"
---

<objective>
Implement **full playlist track retrieval** for Spotify playlists that exceed one API page (**100** tracks max per request) by paging `getPlaylistTracks` with **`limit`** and **`offset`**, and extend **`SpotifyPlaylistTracksResponse`** so paging is type-accurate (**SPOT-01** / ROADMAP **04-01**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-spotify-api-robustness/04-RESEARCH.md
@.cursor/rules/functional-style.mdc
@.cursor/rules/utils-and-modules.mdc
@src/sourceScrappers/spotify-scrapper.ts
@src/types/api.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type playlist track page shape (offset/limit/total/next)</name>
  <read_first>
    - src/types/api.d.ts
    - .planning/phases/04-spotify-api-robustness/04-RESEARCH.md
  </read_first>
  <files>src/types/api.d.ts</files>
  <action>
    In `src/types/api.d.ts`, inside `SpotifyPlaylistTracksResponse.body`, add **optional** fields (all optional so existing casts stay valid):
    - `items` (already present)
    - `next?: string | null`
    - `total?: number`
    - `limit?: number`
    - `offset?: number`
  </action>
  <verify>
    <automated>yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg "next\\?:" src/types/api.d.ts` exits **0**.
    - `rg "SpotifyPlaylistTracksResponse" src/types/api.d.ts` exits **0**.
    - `yarn type-check` exits **0**.
  </acceptance_criteria>
  <done>Response type documents paging fields used by the executor for SPOT-01.</done>
</task>

<task type="auto">
  <name>Task 2: Page through all playlist tracks (limit 100, offset loop)</name>
  <read_first>
    - src/sourceScrappers/spotify-scrapper.ts
    - src/types/api.d.ts
    - .cursor/rules/functional-style.mdc
  </read_first>
  <files>src/sourceScrappers/spotify-scrapper.ts</files>
  <action>
    In `getSpotifyPlaylistTracksInternal`, **after** `setAccessToken` and **before** transforming to string[], replace the single call `spotifyApi.getPlaylistTracks(playlistId)` with a loop:
    - Constants: `const PAGE_SIZE = 100` (literal **100** — Spotify max per request).
    - Initialize `let offset = 0` and `const allItems: SpotifyPlaylistItem[] = []` (import `SpotifyPlaylistItem` from `../types` if not already in scope).
    - `while (true)`: call `(await spotifyApi.getPlaylistTracks(playlistId, { limit: PAGE_SIZE, offset })) as SpotifyPlaylistTracksResponse`.
    - Push each element of `response.body.items` into `allItems` (use spread or `push(...items)`).
    - **Break** when `items.length === 0` OR `items.length < PAGE_SIZE` (end of playlist).
    - Otherwise `offset += PAGE_SIZE` and continue.
    - Add **one** `console.log` line with prefix `[Spotify][API]` reporting **paged fetch** progress, e.g. cumulative track count or page index (must include substring **`offset=`** or **`page`** so logs show paging is active).
    - Map **only** `allItems` to strings: for each item, if `item.track` is null/undefined, **skip** (do not push empty strings).
    - Keep artist/title formatting: `${artists} ${track.name}` unchanged for valid tracks.
    - Leave **error handling** structure as-is for this plan (04-02 will normalize messages); do not remove existing `try/catch` shells.
  </action>
  <verify>
    <automated>yarn type-check</automated>
  </verify>
  <acceptance_criteria>
    - `rg "PAGE_SIZE\\s*=\\s*100" src/sourceScrappers/spotify-scrapper.ts` exits **0**.
    - `rg "getPlaylistTracks\\(playlistId,\\s*\\{\\s*limit:\\s*PAGE_SIZE,\\s*offset" src/sourceScrappers/spotify-scrapper.ts` exits **0** OR `rg "getPlaylistTracks\\(playlistId, \\{ limit: PAGE_SIZE, offset \\}" src/sourceScrappers/spotify-scrapper.ts` exits **0** (whitespace-tolerant: at minimum `rg "limit:\\s*PAGE_SIZE" src/sourceScrappers/spotify-scrapper.ts` exits **0** and `rg "offset" src/sourceScrappers/spotify-scrapper.ts` exits **0** with both near `getPlaylistTracks`).
    - `rg "item\\.track" src/sourceScrappers/spotify-scrapper.ts` exits **0** (guard for null track).
    - `yarn type-check` exits **0**.
  </acceptance_criteria>
  <done>SPOT-01: multi-page playlists aggregate into one `string[]`.</done>
</task>

</tasks>

<verification>
`yarn type-check`. Manual: run against a public playlist with **&gt;100** tracks; track count should exceed 100.
</verification>

<success_criteria>
All playlist pages are fetched under normal API limits; null tracks skipped; types reflect paging metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/04-spotify-api-robustness/04-01-SUMMARY.md`
</output>
