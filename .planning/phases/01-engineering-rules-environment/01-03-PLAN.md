---
phase: 01-engineering-rules-environment
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - .cursor/rules/node-engines.mdc
  - package.json
  - README.md
  - scripts/verify-phase1-rules.mjs
autonomous: true
requirements:
  - QUAL-01
requirements_addressed:
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - "`package.json` engines.node minimum is >=18 aligned with Playwright (D-04)."
    - "README prerequisites state the same minimum; rules cross-link (D-05)."
    - "Automated script fails if rule files missing or engines floor wrong."
  artifacts:
    - path: ".cursor/rules/node-engines.mdc"
      provides: "QUAL-01 Node/engine policy and cross-links"
      contains: "engines"
    - path: "package.json"
      provides: "Authoritative engines.node"
      contains: "\"node\": \">=18\""
    - path: "scripts/verify-phase1-rules.mjs"
      provides: "Phase 1 rules + engines verification"
    - path: "README.md"
      provides: "Contributor-visible Node prerequisite"
  key_links:
    - from: ".cursor/rules/node-engines.mdc"
      to: "package.json"
      via: "states engines.node is source of truth"
    - from: "README.md"
      to: "package.json"
      via: "prerequisite line matches >=18"
---

<objective>
Align Node/engine documentation (QUAL-01) with Playwright’s published minimum (>=18): update `package.json` `engines.node` per D-04, add `node-engines.mdc` cross-linking README and `package.json` per D-05, and ship `scripts/verify-phase1-rules.mjs` with a `package.json` script entry for regression checks.

Purpose: Eliminate drift between README, rules, and `engines` (RESEARCH pitfall 1).

Output: `node-engines.mdc`, updated `package.json` + `README.md`, verification script + npm/yarn script.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-engineering-rules-environment/01-CONTEXT.md
@.planning/phases/01-engineering-rules-environment/01-RESEARCH.md
@.planning/phases/01-engineering-rules-environment/01-VALIDATION.md
@package.json
@README.md
@.cursor/rules/engineering-overview.mdc

Required rule filenames for verification script (must match disk after 01-01 + 01-02):
`_format-pilot.mdc`, `engineering-overview.mdc`, `functional-style.mdc`, `utils-and-modules.mdc`, `hooks-policy.mdc`, `memory-and-concurrency.mdc`, `node-engines.mdc` — all under `.cursor/rules/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: node-engines.mdc and package.json engines (D-04)</name>
  <read_first>
    - package.json
    - .planning/phases/01-engineering-rules-environment/01-CONTEXT.md
    - README.md
    - .cursor/rules/engineering-overview.mdc
  </read_first>
  <files>.cursor/rules/node-engines.mdc,package.json</files>
  <action>
    **package.json:** Replace `"engines"."node"` value `">=10.15.3"` with **exact string** `">=18"` (D-04: floor ≥ max(TypeScript, Playwright); Playwright binds >=18). Leave `yarn` engines line unchanged unless inconsistent with `packageManager` (do not remove `packageManager`).

    **node-engines.mdc:** Frontmatter `description: "Node.js engine policy"`, `alwaysApply: true`. MUST: state **`package.json` `engines.node` is authoritative**; minimum **>=18** for this repo because Playwright (devDependency) declares `node >=18` and TypeScript allows lower — Playwright wins. MUST: recommend Node **20+ LTS** in prose for day-to-day alignment with Playwright support docs (optional sentence, not replacing `>=18` floor). Cross-link: tell contributors to read **README** prerequisites and `package.json` — same number (D-05). AVOID: documenting a different minimum in prose than `engines.node`.

    Keep MUST/AVOID (D-02); ~35 lines target.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('./package.json');if(p.engines.node!=='>='+'18')process.exit(1);const r=fs.readFileSync('.cursor/rules/node-engines.mdc','utf8');['engines.node','package.json','>=18'].forEach(s=>{if(!r.includes(s))process.exit(1);});"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); if(p.engines.node!=='>=18') process.exit(1)"` exits 0.
    - `.cursor/rules/node-engines.mdc` exists and contains substrings `engines.node`, `package.json`, and `>=18`.
    - `node -e "require('fs').readFileSync('package.json','utf8').includes('>=10.15.3')&&process.exit(1)"` exits 0 (legacy engines string removed).
  </acceptance_criteria>
  <done>Authoritative engines bump applied; node rule file documents D-04/D-05 linkage.</done>
</task>

<task type="auto">
  <name>Task 2: README prerequisites cross-link (D-05)</name>
  <read_first>
    - README.md
    - package.json
    - .cursor/rules/node-engines.mdc
  </read_first>
  <files>README.md</files>
  <action>
    Under `## 📦 Requirements` (or equivalent **Requirements** section), add a bullet **after** the Node.js link line (or replace generic Node line) with explicit text: **Minimum: Node.js 18+** (`engines.node` in `package.json`). Add second bullet: **Recommended:** Node.js 20 LTS or newer for Playwright-supported versions. Ensure no statement contradicts `>=18`. Keep FFmpeg / API key bullets intact.

    In `## 🤝 Contributing`, add one sentence: **Use the Node version in `package.json` `engines` — see Requirements above.** (D-05 cross-link without duplicating wrong numbers.)
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('README.md','utf8');if(!t.includes('18'))process.exit(1);if(!t.includes('package.json'))process.exit(1);if(!t.includes('engines'))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains substring `Node.js 18` or `Node.js 18+` (grep-verifiable).
    - `README.md` contains `package.json` and `engines` in the Requirements or Contributing edits.
    - `yarn type-check` exits 0.
  </acceptance_criteria>
  <done>README matches engines floor and points readers at package.json.</done>
</task>

<task type="auto">
  <name>Task 3: verify-phase1-rules.mjs and package.json script</name>
  <read_first>
    - package.json
    - .planning/phases/01-engineering-rules-environment/01-VALIDATION.md
  </read_first>
  <files>scripts/verify-phase1-rules.mjs,package.json</files>
  <action>
    Create `scripts/verify-phase1-rules.mjs` (ESM: use `.mjs` so `node` runs without `"type":"module"`). Script must:
    1. `readFileSync('package.json','utf8')`, `JSON.parse`, read `engines.node`.
    2. Exit **1** if `engines.node` is missing or if the declared range’s **minimum major** is **< 18** (parse leading `>=` or `^`/`~` followed by major version; for `>=18` accept; for `>=10.15.3` reject).
    3. Exit **1** if any of these paths missing: `.cursor/rules/_format-pilot.mdc`, `.cursor/rules/engineering-overview.mdc`, `.cursor/rules/functional-style.mdc`, `.cursor/rules/utils-and-modules.mdc`, `.cursor/rules/hooks-policy.mdc`, `.cursor/rules/memory-and-concurrency.mdc`, `.cursor/rules/node-engines.mdc`.
    4. On success, print `verify-phase1-rules: ok` to stdout and exit 0.

    **package.json:** Add script **`verify:phase1-rules`**: `node scripts/verify-phase1-rules.mjs` (exact key name per VALIDATION.md suggestion).

    Do not add npm dependencies for semver; implement minimal parsing inline.
  </action>
  <verify>
    <automated>node scripts/verify-phase1-rules.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-phase1-rules.mjs` exits 0 after prior tasks’ artifacts exist.
    - `package.json` contains key `"verify:phase1-rules"` with value `node scripts/verify-phase1-rules.mjs`.
    - `yarn verify:phase1-rules` exits 0 (or `npm run verify:phase1-rules` — document yarn in verify).
    - `yarn type-check` exits 0.
  </acceptance_criteria>
  <done>Automated gate for QUAL-01 + rule file presence is wired for CI/local use.</done>
</task>

</tasks>

<verification>
Run `yarn type-check` and `yarn verify:phase1-rules` after all phase 1 rule files from 01-01/01-02 exist.
</verification>

<success_criteria>
engines aligned to Playwright floor; README and rules agree; verification script green on clean tree.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engineering-rules-environment/01-03-SUMMARY.md`
</output>
