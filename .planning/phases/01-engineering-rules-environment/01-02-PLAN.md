---
phase: 01-engineering-rules-environment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .cursor/rules/memory-and-concurrency.mdc
autonomous: true
requirements:
  - GOV-02
requirements_addressed:
  - GOV-02
user_setup: []
must_haves:
  truths:
    - "Rules explain RAM/concurrency: prAll vs caps, Playwright lifecycle, batching guidance."
    - "Ground truth paths from youtubeSearcher, youtubeDownloader, 1001-scrapper are cited."
  artifacts:
    - path: ".cursor/rules/memory-and-concurrency.mdc"
      provides: "GOV-02 memory and concurrency policy"
      contains: "Playwright"
  key_links:
    - from: ".cursor/rules/memory-and-concurrency.mdc"
      to: "src/sourceScrappers/1001-scrapper.ts"
      via: "browser.launch / browser.close documentation"
    - from: ".cursor/rules/memory-and-concurrency.mdc"
      to: "src/youtubeSearcher/index.ts"
      via: "unbounded map + prAll pattern"
---

<objective>
Document memory, parallelism, and Playwright lifecycle expectations (GOV-02) in a dedicated rule file per D-01, using MUST/AVOID lists and pointers into real pipeline code (D-02).

Purpose: Prevent OOM leaks and mistaken assumption that `prAll` limits concurrency (per RESEARCH pitfall 3).

Output: `.cursor/rules/memory-and-concurrency.mdc`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-engineering-rules-environment/01-CONTEXT.md
@.planning/phases/01-engineering-rules-environment/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@src/utils/index.ts
@src/youtubeDownloader/index.ts
@src/youtubeSearcher/index.ts
@src/sourceScrappers/1001-scrapper.ts

<interfaces>
`prAll` aggregates `Promise.allSettled` — it does **not** cap concurrency (RESEARCH).

Playwright in `1001-scrapper.ts`: `chromium.launch`, work, `browser.close()` on success and error paths (grep `browser.close`).

Downloader/searcher: `map` over tracks/URLs building promise arrays, then `prAll(handler)(promises)` — unbounded fan-out at time of Phase 1 research.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author memory-and-concurrency.mdc from ground truth</name>
  <read_first>
    - src/youtubeDownloader/index.ts
    - src/youtubeSearcher/index.ts
    - src/sourceScrappers/1001-scrapper.ts
    - src/utils/index.ts
    - .planning/phases/01-engineering-rules-environment/01-RESEARCH.md
  </read_first>
  <files>.cursor/rules/memory-and-concurrency.mdc</files>
  <action>
    Create `.cursor/rules/memory-and-concurrency.mdc` with frontmatter: `description: "Memory, parallelism, Playwright lifecycle"`, `globs: ["src/**/*.ts"]`, `alwaysApply: false`.

    MUST include:
    - Clarify **`prAll` = settlement aggregation**, not a concurrency pool; cite `src/utils/index.ts` and consumer examples `src/youtubeDownloader/index.ts`, `src/youtubeSearcher/index.ts`.
    - **Playwright:** launch → work → **`browser.close()`** on all paths; cite `src/sourceScrappers/1001-scrapper.ts` (mention `chromium.launch` and both close sites). MUST: avoid leaking browsers; AVOID: spawning unbounded concurrent `chromium.launch` for playlist-scale work without explicit design.
    - **Parallelism / batching:** MUST document current risk: mapper patterns can issue N simultaneous network/download operations; AVOID adding new unbounded `Promise.all` over large N without an explicit cap or batching strategy; prefer documenting direction for future work (batch size / limiter) without rewriting pipeline in this phase.

    Use MUST/AVOID sections (D-02). Keep under ~60 lines unless disambiguation needs a short narrative paragraph (D-02).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='.cursor/rules/memory-and-concurrency.mdc',t=fs.readFileSync(p,'utf8');['prAll','Promise.allSettled','src/utils/index.ts','src/youtubeDownloader/index.ts','src/youtubeSearcher/index.ts','chromium.launch','browser.close','src/sourceScrappers/1001-scrapper.ts','MUST','AVOID'].forEach(s=>{if(!t.includes(s))process.exit(1);});"</automated>
  </verify>
  <acceptance_criteria>
    - File `.cursor/rules/memory-and-concurrency.mdc` exists.
    - `node -e` substring checklist above exits 0.
    - File content includes the substring `Promise.allSettled` (same check as verify block).
    - `yarn type-check` exits 0.
  </acceptance_criteria>
  <done>GOV-02 satisfied with grep-verifiable citations to downloader, searcher, scraper, and utils.</done>
</task>

</tasks>

<verification>
Confirm `engineering-overview.mdc` from plan 01-01 already lists `memory-and-concurrency.mdc` (no edit required in 01-02 if 01-01 complete).
</verification>

<success_criteria>
Single rule file documents concurrency semantics, Playwright lifecycle, and batching/cap guidance aligned with RESEARCH pitfalls.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engineering-rules-environment/01-02-SUMMARY.md`
</output>
