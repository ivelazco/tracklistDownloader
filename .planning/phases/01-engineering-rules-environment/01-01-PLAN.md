---
phase: 01-engineering-rules-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .cursor/rules/_format-pilot.mdc
  - .cursor/rules/engineering-overview.mdc
  - .cursor/rules/functional-style.mdc
  - .cursor/rules/utils-and-modules.mdc
  - .cursor/rules/hooks-policy.mdc
autonomous: true
requirements:
  - GOV-01
  - GOV-03
  - GOV-04
requirements_addressed:
  - GOV-01
  - GOV-03
  - GOV-04
user_setup: []
must_haves:
  truths:
    - "Cursor rule format (.mdc) is chosen and recorded in-repo for this project."
    - "A thin overview rule links to topic-split rules (per D-01)."
    - "FP, utils, and hooks policies are prescriptive (MUST/AVOID) with canonical src/ pointers (per D-02, D-03)."
  artifacts:
    - path: ".cursor/rules/_format-pilot.mdc"
      provides: "Pilot rule file + format decision note"
      contains: ".mdc"
    - path: ".cursor/rules/engineering-overview.mdc"
      provides: "Index linking to GOV topic rules"
    - path: ".cursor/rules/functional-style.mdc"
      provides: "GOV-01 Ramda/pipe/prAll/guards conventions"
      contains: "prAll"
    - path: ".cursor/rules/utils-and-modules.mdc"
      provides: "GOV-03 src/utils and barrel boundaries"
      contains: "src/utils"
    - path: ".cursor/rules/hooks-policy.mdc"
      provides: "GOV-04 CLI-only hooks N/A + extension clause"
      contains: "CLI"
  key_links:
    - from: ".cursor/rules/functional-style.mdc"
      to: "src/utils/index.ts"
      via: "documented canonical import for prAll/tapAfter"
    - from: ".cursor/rules/utils-and-modules.mdc"
      to: "src/handler.ts"
      via: "barrel import pattern"
---

<objective>
Establish Cursor project rules for functional style (GOV-01), shared utils and module boundaries (GOV-03), and hooks policy (GOV-04), split across multiple `.mdc` files per D-01. Confirm `.mdc` + YAML frontmatter as the repo standard via Wave 0 pilot (addresses RESEARCH open question).

Purpose: Ground agents and contributors in existing `src/` idioms (Ramda, `prAll`, `tapAfter`, barrels) without essay-only rules (D-02).

Output: Five rule files under `.cursor/rules/` (pilot + overview + three topic files).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-engineering-rules-environment/01-CONTEXT.md
@.planning/phases/01-engineering-rules-environment/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@src/utils/index.ts
@src/handler.ts

<interfaces>
From `src/utils/index.ts`:
```typescript
const prAll = <T, R>(fnReturn: PromiseSettledHandler<T, R>) => (ps: Promise<T>[]) =>
  Promise.allSettled(ps).then(fnReturn);
const curriedTapAfter = curryN(3, tapAfter);
export { prAll, curriedTapAfter as tapAfter, /* print helpers */ };
```

From `src/handler.ts`:
```typescript
import ytDownloader from './youtubeDownloader';
import getTracklist from './sourceScrappers';
import searchYtVideos from './youtubeSearcher';
import createFolder from './folderManager';
import { isNilOrEmpty } from '@flybondi/ramda-land';
export default handler;
```
Document barrels: feature folders expose `index.ts`; orchestrator imports default exports from those barrels.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pilot `.mdc` rule format and record decision</name>
  <read_first>
    - .planning/phases/01-engineering-rules-environment/01-RESEARCH.md
    - https://docs.cursor.com/context/rules (if local fetch fails, use RESEARCH.md summary)
  </read_first>
  <files>.cursor/rules/_format-pilot.mdc</files>
  <action>
    Create `.cursor/rules/_format-pilot.mdc` with YAML frontmatter exactly containing keys: `description: "Phase 1 pilot — Cursor rule file format for tracklistDownloader"` and `alwaysApply: false` (pilot only; avoid polluting all chats). Body must include a **Format decision** subsection with verbatim sentence: `Repository standard: Cursor project rules use .mdc files with YAML frontmatter (description, alwaysApply, optional globs).` and `Pilot created for Phase 1 plan 01-01; executor confirmed extension .mdc is used.` If Cursor rejects `.mdc`, stop and switch to `.md` only after documenting the editor error in the same file under **Format decision** — default path is **stay on .mdc** per RESEARCH unless verification fails.

    Per D-02: keep body under ~25 lines; no essay.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='.cursor/rules/_format-pilot.mdc';if(!fs.existsSync(p))process.exit(1);const t=fs.readFileSync(p,'utf8');if(!/^---\r?\n[\s\S]*?\r?\n---/.test(t))process.exit(1);if(!t.includes('alwaysApply:'))process.exit(1);if(!t.includes('Repository standard:'))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - File `.cursor/rules/_format-pilot.mdc` exists.
    - File content matches `node -e` check above (exit code 0).
    - Grep `Repository standard:` inside file: present (e.g. `rg "Repository standard:" .cursor/rules/_format-pilot.mdc` exit 0).
  </acceptance_criteria>
  <done>Pilot `.mdc` exists; format decision text is grep-verifiable; frontmatter parses as YAML delimited block.</done>
</task>

<task type="auto">
  <name>Task 1: Engineering overview rule (index)</name>
  <read_first>
    - .cursor/rules/_format-pilot.mdc
    - .planning/phases/01-engineering-rules-environment/01-CONTEXT.md
  </read_first>
  <files>.cursor/rules/engineering-overview.mdc</files>
  <action>
    Create `.cursor/rules/engineering-overview.mdc` with frontmatter: `description: "TracklistDownloader — engineering rules index"`, `alwaysApply: true`. Body: MUST list relative links or bullet paths to these files (create stubs as “see plan 01-02/01-03” only if file missing — **prefer listing final names**): `functional-style.mdc`, `utils-and-modules.mdc`, `hooks-policy.mdc`, `memory-and-concurrency.mdc`, `node-engines.mdc`. One-line purpose per bullet. Per D-01: this file is only an index, not a monolith duplicating other topics.

    Explicitly state: rules are split by topic; agents should read the relevant file before editing matching `src/` areas.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p='.cursor/rules/engineering-overview.mdc';const t=fs.readFileSync(p,'utf8');['functional-style.mdc','utils-and-modules.mdc','hooks-policy.mdc','memory-and-concurrency.mdc','node-engines.mdc'].forEach(f=>{if(!t.includes(f))process.exit(1);});"</automated>
  </verify>
  <acceptance_criteria>
    - `.cursor/rules/engineering-overview.mdc` exists.
    - `node -e` substring check for all five filenames exits 0.
    - Frontmatter includes `alwaysApply: true` (grep `alwaysApply: true` exit 0).
  </acceptance_criteria>
  <done>Index file lists all topic rule filenames and uses alwaysApply.</done>
</task>

<task type="auto">
  <name>Task 2: FP, utils, and hooks policy rules</name>
  <read_first>
    - .planning/codebase/CONVENTIONS.md
    - src/utils/index.ts
    - src/handler.ts
    - .cursor/rules/engineering-overview.mdc
  </read_first>
  <files>.cursor/rules/functional-style.mdc .cursor/rules/utils-and-modules.mdc .cursor/rules/hooks-policy.mdc</files>
  <action>
    **functional-style.mdc (GOV-01, D-02):** Frontmatter `description: "Functional style — Ramda, prAll, guards"`, `globs: ["src/**/*.ts"]`, `alwaysApply: false`. MUST bullets: use `pipe`/`compose` and `@flybondi/ramda-land` helpers (`isNilOrEmpty`, etc.) consistent with `src/handler.ts`; use **`prAll`** for batch async where outcomes are aggregated with `Promise.allSettled` (not raw `Promise.all` for mixed success/failure batches); reference **`tapAfter`** for post-run logging wrappers (see `src/sourceScrappers/1001-scrapper.ts` export pattern in CONVENTIONS). AVOID: imperative sprawl in new modules without extracting cohesive functions (per CONVENTIONS). Include pointers: `src/utils/index.ts`, `src/handler.ts`, `src/youtubeDownloader/index.ts`, `src/youtubeSearcher/index.ts`.

    **utils-and-modules.mdc (GOV-03, D-02):** Frontmatter `description: "Shared utils and module boundaries"`, `globs: ["src/**/*.ts"]`, `alwaysApply: false`. MUST: add cross-cutting helpers under `src/utils/` and re-export from `src/utils/index.ts`; import feature code via folder barrels (`import x from './featureFolder'`); keep `src/types/` for shared ambient types. MUST: **naming** — follow existing `src/` conventions (camelCase functions/modules, match `CONVENTIONS.md` naming guidance). MUST: **errors / reporting** — use `console` with bracketed namespaces (e.g. `[handler]`) as in existing modules; do not introduce a new logger abstraction unless scoped in a later phase; point readers to `CONVENTIONS.md` for logging/error posture. AVOID: deep relative imports past public barrel without reason. Pointers: `src/utils/index.ts`, `src/utils/printUtils.ts`, `src/handler.ts`, STRUCTURE “Where to Add New Code”.

    **hooks-policy.mdc (GOV-04, D-03):** Frontmatter `description: "React hooks policy (CLI-first)"`, `alwaysApply: true`. MUST state verbatim clause: `CLI-only codebase today — React hooks N/A.` Extension (brief): if a UI layer is added, add dedicated hooks rules under `.cursor/rules/` and link from `engineering-overview.mdc`; do not add full React style guides preemptively. AVOID: introducing hooks in `src/` until a UI exists.

    All three: use **MUST** / **AVOID** section headers (D-02).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['.cursor/rules/functional-style.mdc','.cursor/rules/utils-and-modules.mdc','.cursor/rules/hooks-policy.mdc'];const need=[['prAll','src/utils/index.ts'],['src/utils/index.ts','barrel','CONVENTIONS.md','naming'],['CLI-only codebase today']];for(let i=0;i<files.length;i++){const t=fs.readFileSync(files[i],'utf8');need[i].forEach(s=>{if(!t.includes(s))process.exit(1);});}if(!fs.readFileSync(files[1],'utf8').includes('MUST'))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist at paths above.
    - `functional-style.mdc` contains substring `prAll` and `src/utils/index.ts`.
    - `utils-and-modules.mdc` contains `src/utils/index.ts`, substring `naming`, substring `CONVENTIONS.md`, and header text `MUST`.
    - `hooks-policy.mdc` contains exact phrase `CLI-only codebase today — React hooks N/A.`
    - `yarn type-check` exits 0 (no accidental TS edits; run from repo root).
  </acceptance_criteria>
  <done>GOV-01/03/04 captured in scannable rules with canonical paths; D-03 clause present verbatim.</done>
</task>

</tasks>

<verification>
Run `yarn type-check`. Spot-check that overview lists filenames that match plans 01-02 and 01-03 outputs.
</verification>

<success_criteria>
Pilot format documented; overview + three topic rules exist; every locked decision D-01, D-02, D-03 for this slice is reflected in file content (grep-verifiable).
</success_criteria>

<output>
After completion, create `.planning/phases/01-engineering-rules-environment/01-01-SUMMARY.md`
</output>
